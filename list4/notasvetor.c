#include<stdio.h>
int main(){
int N, i, j, h, a, b, c, x;
c=0;
   scanf("%d", &N);
   int v[N], contador[N];
   for(i=0;i<N;i++){
   	  scanf("%d", &v[i]);
   }
   for(i=0;i<N;i++){
    	for(j=1;j<N;j++){
	       if(v[i]==v[j]){
	    	  c=c+1;
	       }
	
		}
   contador[x]=contador[x] + c;
   if(contador[x] > contador[x]){
   	 a=v[i];
   }
   else if (contador[x]==contador[x]){
   	    if ()
   }			
   c=0;
   
   }
   printf("%d\n", a);
return 0;
}
